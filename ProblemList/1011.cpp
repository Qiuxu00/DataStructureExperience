#include <bits/stdc++.h>
using namespace std;

void rec()
{
}

void solve(vector<vector<int>> &res)
{
    int n;
    vector<pair<int, int>> nodes(n);
    for (auto &p : nodes)
    {
        cin >> p.first;
        cin >> p.second;
    }
}

int main()
{
    int n;
    cin >> n;
    vector<vector<int>> res;
    for (int i = 0; i < n; i++)
    {
        solve(res);
    }
    return 0;
}