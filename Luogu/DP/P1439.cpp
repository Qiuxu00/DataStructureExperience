#include <bits/stdc++.h>
using namespace std;
int main()
{
    vector<int> A;
    vector<int> B;
    vector<vector<int>> dp(B.size(), vector<int>(A.size()));
    dp[0][0] = (A[0] == B[0]);
    for (int i = 0; i < B.size(); i++)
    {
    }
}